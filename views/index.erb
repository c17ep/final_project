<!doctype HTML>
<html lang="en">
	<head> </head>
	<body>
		<h1>Choose music for your moods!</h1>
		<!-- enter button -->
		<form action="/picksong" method="POST"> 

		<% @moods_arr.each do |t| %>
			<input type="radio" name="<%= t %>" value="<%= t %>"> <%= t %> <br>
		<% end %>
		<input type="submit">
		</form>
	</body>
</html>